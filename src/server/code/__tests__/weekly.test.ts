import { weeklyCode } from '../weekly';

describe('weeklyCode', () => {
  it('should return weekly code data', async () => {
    // const result = await weeklyCode();
    expect(true).toBeDefined();
  });
});
